Corrige el siguiente error en el proyecto Next.js:

Type error: Property 'variant' is missing in type '{ key: string; newsItem: Article; index: number; }' but required in type 'NewsCardProps'.


Detalles del proyecto:

Framework: Next.js 14.2.33

Lenguaje: TypeScript

Archivo con error: /src/app/categoria/[categoria]/page.tsx

Línea con error:

<NewsCard 
  key={noticia.id} 
  newsItem={noticia} 
  index={index}
/>

Instrucciones para Gemini

Abre el archivo /src/app/categoria/[categoria]/page.tsx.

Detecta el lugar donde se renderiza el componente NewsCard.

Analiza el tipo NewsCardProps en el componente NewsCard (debería estar en /src/components/NewsCard.tsx o similar).

Si la prop variant es obligatoria, agrega un valor por defecto seguro.
Ejemplo:

<NewsCard
  key={noticia.id}
  newsItem={noticia}
  index={index}
  variant="default"
/>

Usa "default" o "standard" según los tipos definidos en NewsCardProps.

Si variant debería ser opcional, modifica la definición del tipo NewsCardProps agregando ?:

variant?: string;


Asegúrate de que el proyecto compile correctamente (npm run build) y que el cambio no afecte el comportamiento visual del componente.

Limpia cualquier importación o código innecesario detectado.

Optimiza el código resultante.