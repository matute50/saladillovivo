Corrige el siguiente error de tipado en el proyecto Next.js (TypeScript):

Type error: Type '{ featuredVideo: null; recentVideos: never[]; videoCategories: never[]; allVideos?: undefined; }'
is not assignable to type '{ featuredVideo: Video | null; recentVideos: Video[]; allVideos: Video[]; videoCategories: string[]; }'

Detalles del proyecto:

Framework: Next.js 14.2.33

Lenguaje: TypeScript

Archivo con error: /src/app/page.tsx

Línea aproximada: 57

Instrucciones para Gemini

Abre /src/app/page.tsx y localiza la definición del objeto videos dentro del objeto PageData o de la función getPageData o similar.

Detecta si en algún caso allVideos no está definido (undefined).

Modifica el código para garantizar que allVideos siempre tenga un arreglo, aunque esté vacío.
Por ejemplo, cambia:

allVideos: allVideos,

por:

allVideos: allVideos ?? [],


o agrega allVideos: [] en los retornos donde falte.

Asegúrate de que el tipo PageData y sus subtipos (Video, etc.) sigan siendo consistentes.

Ejecuta npm run build localmente para verificar que el error desaparece.

Limpia cualquier declaración duplicada o importación innecesaria.

Optimiza el código final para mantener compatibilidad con el tipado estricto de TypeScript.